# Copy this to ~/.activitytracker/secrets.env on each laptop and fill in your key.
# Then lock it down:  chmod 600 ~/.activitytracker/secrets.env
# The LaunchAgent / run-tracker.sh sources this so the API key never lives in the plist.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxx
RESEND_TO=dharamarao.bala@manabadi.siliconandhra.org
RESEND_FROM=onboarding@resend.dev
